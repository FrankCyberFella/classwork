// This interface describes a Gambler object
// we export it so others can use it
export interface GamblerDataLayout {
    id        : number
    address   : string
    name      : string
    birthDate : string
    salary    : number
}